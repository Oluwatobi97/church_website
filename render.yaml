services:
  - type: web
    name: church-website
    buildCommand: npm run build
    startCommand: npm install -g serve && serve -s dist -l 3000
    envVars:
      - key: NODE_ENV
        value: production
